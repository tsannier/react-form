import React, { PropTypes } from 'react';


class Checkbox extends React.Component {

  static propTypes = {
  };

  constructor(props){
      super(props);
  }

  render() {
      <div>
          Checkbox
      </div>
  }
}

export default Checkbox; 
